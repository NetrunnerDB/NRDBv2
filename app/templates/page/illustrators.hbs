{{page-title "Illustrators"}}

<div class="container">
  <div class="row">
    <div
      class="col-12 col-xl-8 offset-xl-2 d-flex flex-row flex-sm-column my-2"
    >
      <div class="card border-0 flex-grow-1" id="nav-main-content">
        <div
          class="card-body d-flex flex-column flex-lg-row justify-content-between"
        >
          <div class="container">
            <div class="row">
              <h1>Illustrators</h1>
              <p>The world of Netrunner has been brought to life by a wide
                selection of amazing artists! This page lists all artists whose
                work has appeared on cards and which cards they illustrated.</p>
            </div>
            <div class="row">
              <PanelList
                @showSearch="true"
                @showSort="true"
                @mutuallyExclusive="true"
              >
                {{#each @model as |illustrator|}}
                  <Panel
                    @title="{{illustrator.name}} — ({{illustrator.numPrintings}} card{{if
                      (not-eq illustrator.numPrintings 1)
                      's'
                    }})"
                    @subtitle=""
                  >
                    <div class="container">
                      <div class="row">
                        {{#each illustrator.printings as |printing|}}
                          <ul class="col-4">
                            <li>
                              <Card::LinkTo @printing={{printing}}>
                                {{printing.title}}
                              </Card::LinkTo>
                            </li>
                          </ul>
                        {{/each}}
                      </div>
                    </div>
                  </Panel>
                {{/each}}
              </PanelList>
            </div>
          </div>

        </div>
      </div>
    </div>
  </div>
</div>