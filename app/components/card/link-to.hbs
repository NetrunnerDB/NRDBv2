{{!-- TODO: is there a way to programatically pass all arguments from this component to the child LinkTo? --}}
<LinkTo @model={{@model.id}} ...attributes>
  {{yield}}
  <Tippy @placement="auto" @theme="light-border" as |tippy|>
    <Card::TextBox @printing={{@model}} @showTitle={{true}} @showThumbnail={{true}} />
  </Tippy>
</LinkTo>